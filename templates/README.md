# Report templates

Runtime templates are packaged under `src/retirement_engine/templates` so installed wheels remain self-contained. This directory is reserved for future user-overridable templates.
